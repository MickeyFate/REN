Описание задачи:
Ребята, когда работают с ИД, заполняют две таблицы Excel (КС6 а (02.04.2025) и 1. База ИД (18.06.2025)).
Иван хочет как-то упростить процесс, автоматизировать, где это возможно.
В частности, заполнение выполненных объёмов работ по контрактам. Ребята вносят подписанные объёмы по ИД-шкам в обе таблицы в разных форматах.

Наброски:
А. Сделать так, чтобы ребята вбивали данные только в одну таблицу, а другой файл по макросу нужную инфу находил (в нужных столбцах, по заранее выбранному параметру)
и по определённому формату вставлял в свои ячейки.

Работаем.

А.
1. Мы выбрали с Иваном параметр для подтяжки данных - id расценка. Ребята будут её вбивать на постоянной основе в первую таблицу, со второй же, по идее, должно стать меньше
мороки. Таким образом нам нужно добавить новый столбец. И важно! Чтобы после добавления этого столбца, у нас не поехали макросы. Имеем:
1) [+] Добавляем столбец "id расценки"
2) [+] Вручную проверяем, чтобы макросы не слетели, при необходимости - корректируем.
Корректировать ничего не пришлось, столбец не повлияет на работу макросов.
3) [-] Тестируем при необходимости
4) [+] Пишем макрос 

2. Нужно добавить фильтрацию в макросе ещё и по дате. Чтоб за определённый месяц


А может вообще всё это организовать через сводную таблицу?
По идее, можно отфильтровать через id расценки. Закинуть в фильтры
Не, там в таблице изначально ведётся всё по каждому месяцу. В сводной без столбцов 